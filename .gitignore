# Dependencies
node_modules
.pnp
.pnp.js

# Build output
dist
dist-ssr
*.local

# tsc incremental + composite project artifacts
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Local env files — never commit real values
.env
.env.local
.env.*.local

# Firebase Web config (gitignored hygiene — values are public but kept out
# of source). See src/lib/firebase-config.example.ts for the template.
src/lib/firebase-config.ts
